name: CI/CD for non-main branches

on:
    push:
        branches-ignore:
            - main

jobs:
    build:
        name: Build & Test
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3
            - run: npm install
            - run: npm test
